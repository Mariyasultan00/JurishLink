import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Judgement4Page } from './judgement4.page';

describe('Judgement4Page', () => {
  let component: Judgement4Page;
  let fixture: ComponentFixture<Judgement4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Judgement4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
