import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Lawyer6Page } from './lawyer6.page';

describe('Lawyer6Page', () => {
  let component: Lawyer6Page;
  let fixture: ComponentFixture<Lawyer6Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Lawyer6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
